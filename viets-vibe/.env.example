# Google AI Studio API key (server-side only)
GEMINI_API_KEY=
# Optional: choose a Gemini model available to your Google project
GEMINI_MODEL=gemini-flash-latest
# Used only when the main Google model is busy or times out
GEMINI_FALLBACK_MODEL=gemini-flash-lite-latest
